public class StepTracker {
/*
    double[] monthData = new double[11];
    int stepWriter (int month, int day, int spet)
    monthData[month]
*/
   monthToData[1].day[1] = 333;

        public StepTracker() {
            monthToData = new StepTracker.MonthData[12];
            for (int i = 0; i < monthToData.length; i++) {
                if (mo=edd){                }
                monthToData[i] = new StepTracker.MonthData();
                System.out.println();
                оорр = оло - рорлр
            }
        }
    public StepTracker(int t) {
    }

        class MonthData {
            StepTracker.DayData[] dayToData;

            public MonthData() {
                dayToData = new StepTracker.DayData[30];
                for (int i = 0; i < dayToData.length; i++) {
                    dayToData[i] = new StepTracker.DayData();
                }
            }
        }

        class DayData {
        int step = 23;
    }
}